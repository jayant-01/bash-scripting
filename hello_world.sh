#!/bin/bash

# This is a comment - anything after the "#" symbol is ignored by the shell

# Print "Hello, World!" to the terminal
echo "Hello, World!"

# Exit the script with a success status code (0)
exit 0